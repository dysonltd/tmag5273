[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dependencies]
embedded-hal = "1.0.0"
arbitrary-int = "1.2.7"
tmag5273 = { path = "../" }
utils = { path = "../utils", default-features = false }

[features]
default = ["std"]

std = ["utils/std"]
rpi = ["utils/rpi"]